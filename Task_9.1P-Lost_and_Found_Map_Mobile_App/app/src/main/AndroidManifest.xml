<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allows the app to access accurate GPS-based current location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Allows the app to access approximate location if fine location is unavailable. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required for image picking on older Android versions -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:theme="@style/Theme.Task71LostAndFoundApp"
        android:label="Lost and Found App"
        android:supportsRtl="true">

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapActivity" />
        <activity android:name=".ItemDetailActivity" />
        <activity android:name=".ItemListActivity" />
        <activity android:name=".CreateAdvertActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>